# Чек-лист завершения задачи

## Перед коммитом

### 1. Валидация кода
```bash
npm run validate  # Если не только документация
```

**Включает:**
- ✅ Линтинг (ESLint)
- ✅ Проверка типов (TypeScript)
- ✅ Форматирование (Prettier check)
- ✅ Тесты (Vitest, coverage ≥80%)
- ✅ Архитектурные правила (dependency-cruiser)
- ✅ Регистрация tools/operations
- ✅ Безопасность (Knip, Socket.dev, lockfile, secrets)
- ✅ Сборка (build + bundle)
- ✅ Лимиты документации

### 2. Проверка кода
- [ ] Все TODO в коде закрыты
- [ ] Нет `any`, `unknown`, `null`, `undefined` (где можно избежать)
- [ ] Все функции имеют явные типы
- [ ] Импорты используют @ алиасы, НЕ `../`
- [ ] Расширения импортов `.js`, НЕ `.ts`

### 3. Документация
- [ ] CLAUDE.md актуален (если изменилась архитектура/правила)
- [ ] ARCHITECTURE.md актуален (если изменилась архитектура)
- [ ] Module README.md актуален (если изменился модуль)
- [ ] Лимиты размера соблюдены:
  - CLAUDE.md ≤350 строк
  - ARCHITECTURE.md ≤600 строк
  - Module README.md ≤500 строк

### 4. Тесты
- [ ] Unit тесты написаны для нового кода
- [ ] Coverage ≥80%
- [ ] При багфиксе добавлен тест, воспроизводящий баг
- [ ] Все тесты проходят: `npm run test`

### 5. Форматирование
- [ ] ❌ НЕ форматируй вручную
- [ ] ✅ Pre-commit hook сделает автоматически

## Добавление нового Tool

### 1. Создание файлов
```
src/mcp/tools/{api|helpers}/{feature}/{action}/
├── {action}-{feature}.schema.ts
├── {action}-{feature}.definition.ts
├── {action}-{feature}.tool.ts
└── index.ts
```

### 2. Реализация
- [ ] `static readonly METADATA: StaticToolMetadata`
  - [ ] ⚠️ Если ИЗМЕНЯЕТ данные → `requiresExplicitUserConsent: true`
  - [ ] ✅ Если только ЧИТАЕТ → НЕ добавляй флаг
- [ ] В `Definition.build()`:
  - [ ] `getStaticMetadata()` → возврат `ToolClass.METADATA`
  - [ ] `wrapWithSafetyWarning(this.buildDescription())`
- [ ] Используй утилиты:
  - [ ] `validateParams()` для валидации
  - [ ] `BatchResultProcessor` для batch-результатов
  - [ ] `ResultLogger` для логирования
  - [ ] `ResponseFieldFilter.filter()` перед возвратом

### 3. Регистрация
- [ ] Добавить **1 строку** в `src/composition-root/definitions/tool-definitions.ts`
- [ ] `npm run build` (автоматически запустит `generate:index`)

### 4. Проверка
- [ ] `npm run validate:tools` — проверит регистрацию и флаги
- [ ] `npm run test` — запустить тесты
- [ ] `npm run validate` — полная валидация

## Добавление новой Operation

### 1. Создание файла
```
src/tracker_api/api_operations/{feature}/{action}/{name}.operation.ts
```

### 2. Реализация
- [ ] Наследуется от `BaseOperation`
- [ ] Метод `execute(...)` реализован
- [ ] Для batch: использует `ParallelExecutor`
- [ ] Для batch: возвращает `BatchResult<T>`
- [ ] Правильные типы Entity/DTO:
  - [ ] Входящие данные → `{Name}WithUnknownFields`
  - [ ] Исходящие данные → строгий DTO

### 3. Facade
- [ ] Добавить метод в `YandexTrackerFacade`
- [ ] Использует operation через DI

### 4. Регистрация
- [ ] Добавить **1 строку** в `src/composition-root/definitions/operation-definitions.ts`

### 5. Проверка
- [ ] `npm run validate:tools` — проверит регистрацию
- [ ] `npm run test` — запустить тесты
- [ ] `npm run validate` — полная валидация

## Добавление Entity

### 1. Создание
```typescript
// src/tracker_api/entities/{name}.entity.ts
export interface {Name} {
  // только known поля
}

export type {Name}WithUnknownFields = WithUnknownFields<{Name}>;
```

### 2. Экспорт
- [ ] Добавить в `src/tracker_api/entities/index.ts`

## Добавление DTO

### 1. Создание
```typescript
// src/tracker_api/dto/{feature}/{name}.dto.ts
export interface {Name}Dto {
  // поля для API запроса
  [key: string]: unknown; // опционально для кастомных полей
}
```

### 2. Update DTO
- [ ] Все поля опциональны

### 3. Экспорт
- [ ] Добавить в `src/tracker_api/dto/index.ts`

## Исправление бага

### 1. Воспроизведение
- [ ] Создать failing test, воспроизводящий баг

### 2. Исправление
- [ ] Исправить баг в коде
- [ ] Тест должен пройти

### 3. Документация
- [ ] Обновить документацию при необходимости

### 4. Проверка
- [ ] `npm run validate`

## Рефакторинг

### 1. Без изменения API
- [ ] Тесты остаются зелёными
- [ ] Coverage не снижается
- [ ] ❌ НЕ обновляй документацию

### 2. С изменением API
- [ ] Обновить все использования
- [ ] Обновить тесты
- [ ] Обновить документацию (если требуется)

### 3. Проверка
- [ ] `npm run validate`
- [ ] `npm run depcruise` — архитектурные правила

## Обновление документации

### 1. Когда обновлять README.md
- [ ] Добавление модуля/компонента
- [ ] Изменение архитектуры/API
- [ ] Добавление правил/ограничений

### 2. Когда НЕ обновлять
- [ ] Рефакторинг без изменения API
- [ ] Исправление багов
- [ ] Обновление комментариев

### 3. Проверка лимитов
```bash
npm run validate:docs
```

## Автоматический коммит

### Когда можно коммитить автоматически
1. ✅ Задача завершена (все TODO выполнены)
2. ✅ `npm run validate` успешна
3. ✅ Нет вопросов к пользователю

### Формат коммита
```
<тип>: описание

Детали:
- Что/почему/преимущества

🤖 Generated with Claude Code
Co-Authored-By: Claude <noreply@anthropic.com>
```

### Лимиты
- Заголовок ≤72 символа
- Body ≤100 символов на строку

## Pre-commit автоматизация

**Pre-commit hook автоматически выполнит:**
- ✅ Prettier форматирование staged файлов
- ✅ Gitleaks проверка секретов
- ✅ ESLint на staged файлах

**Не нужно делать вручную:**
- ❌ `npm run format`
- ❌ `npm run audit:secrets` на staged
