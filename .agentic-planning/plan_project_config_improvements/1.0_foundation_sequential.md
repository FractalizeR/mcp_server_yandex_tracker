# 1.0 Базовые изменения (SEQUENTIAL)

**Приоритет:** 🔴 Высокий
**Режим выполнения:** ПОСЛЕДОВАТЕЛЬНО (один агент)
**Расчетное время:** 30-45 минут

---

## Цель

Подготовить базу для последующих улучшений: установить необходимые пакеты, создать базовые конфигурационные файлы.

---

## Задачи

### 1.1 Обновить зависимости для современных инструментов

```bash
# В корневом package.json добавить/обновить devDependencies:
npm install -D \
  @eslint/config-inspector \
  globals \
  vitest-workspace \
  @vitest/ui
```

**Обоснование:**
- `@eslint/config-inspector` - для отладки ESLint конфигурации (рекомендация 2025)
- `globals` - стандартный пакет для глобальных переменных вместо ручного определения
- `vitest-workspace` - для правильной настройки monorepo
- `@vitest/ui` - удобный UI для тестов

**Проверка:**
```bash
npm list @eslint/config-inspector globals
```

### 1.2 Создать .editorconfig

**Создать файл:** `.editorconfig` в корне проекта

```ini
# EditorConfig is awesome: https://EditorConfig.org

root = true

[*]
charset = utf-8
end_of_line = lf
insert_final_newline = true
trim_trailing_whitespace = true
indent_style = space
indent_size = 2

[*.{ts,js,mjs,cjs}]
indent_size = 2

[*.{json,jsonc}]
indent_size = 2

[*.md]
trim_trailing_whitespace = false
max_line_length = off

[*.{yml,yaml}]
indent_size = 2

[Makefile]
indent_style = tab
```

**Обоснование:**
- Обеспечивает consistency между разными редакторами/IDE
- Best practice для любого современного проекта
- Синхронизирован с Prettier настройками

**Проверка:**
- Файл создан и синхронизирован с Prettier

### 1.3 Создать .nvmrc

**Создать файл:** `.nvmrc` в корне проекта

```
22
```

**Обоснование:**
- package.json указывает `"node": ">=22.0.0"`
- .nvmrc фиксирует версию для всех разработчиков
- Используется nvm, fnm и другими менеджерами версий Node.js

**Проверка:**
```bash
cat .nvmrc
```

### 1.4 Добавить скрипты в корневой package.json

**Добавить в `scripts` секцию:**

```json
{
  "scripts": {
    "deps:check": "npx npm-check-updates -u --deep --dep dev,prod",
    "deps:audit": "npm audit --audit-level=moderate",
    "eslint:inspect": "eslint --inspect-config",
    "vitest:ui": "vitest --ui --coverage"
  }
}
```

**Обоснование:**
- `deps:check` - проверка устаревших зависимостей
- `deps:audit` - проверка безопасности зависимостей
- `eslint:inspect` - запуск inspector для отладки ESLint
- `vitest:ui` - удобный UI для запуска тестов

**Проверка:**
```bash
npm run deps:check -- --help
```

---

## Чек-лист выполнения

- [ ] 1.1 Установить новые dev dependencies
- [ ] 1.2 Создать .editorconfig
- [ ] 1.3 Создать .nvmrc
- [ ] 1.4 Добавить новые скрипты в package.json
- [ ] Запустить `npm run validate:quiet`
- [ ] Закоммитить изменения: `build: add foundation for config improvements`

---

## Проблемы и решения

**Проблема:** npm-check-updates может обновить пакеты до breaking версий
**Решение:** Использовать с флагом -u только после review изменений

**Проблема:** .nvmrc может конфликтовать с другими tools
**Решение:** Использовать версию без patch (только major.minor или major)
