# Этап 1.1: Создание автоматических тестов валидации (Parallel)

**Приоритет:** Высокий
**Тип выполнения:** Parallel (можно выполнять параллельно с этапами 1.2-1.4)
**Время:** 2-3 часа
**Зависимости:** Требует завершения этапа 0.1

---

## 🎯 Цель

Создать автоматические тесты, которые проверяют соответствие между Zod schema и MCP definition для всех инструментов. Тесты должны выявлять несоответствия на этапе разработки.

---

## ✅ Чек-лист задач

### 1. Создание утилиты для валидации schema-definition

- [ ] Создать файл `packages/servers/yandex-tracker/tests/helpers/schema-definition-validator.ts`
- [ ] Реализовать функцию `validateSchemaDefinitionMatch(schema, tool, expectedRequired)`
  - Извлекать поля из Zod schema через `schema.shape`
  - Получать definition через `tool.getDefinition()`
  - Сравнивать списки полей
  - Проверять required vs optional
  - Возвращать объект с результатами валидации и списком ошибок
- [ ] Добавить поддержку различных типов Zod schema:
  - `z.object()`
  - Nested schemas
  - Union types (опционально)
  - Array types (опционально)

### 2. Создание универсального теста для всех инструментов

- [ ] Создать файл `packages/servers/yandex-tracker/tests/tools/schema-definition-consistency.test.ts`
- [ ] Реализовать автоматическое обнаружение всех инструментов:
  - Сканировать директорию `src/tools/api/`
  - Находить пары `*.schema.ts` + `*.tool.ts`
  - Динамически импортировать схемы и инструменты
- [ ] Создать параметризованный тест для каждого инструмента:
  - Использовать `describe.each()` или `it.each()` для генерации тестов
  - Для каждой пары schema-tool вызывать валидатор
  - Проверять, что все required поля из schema присутствуют в definition
  - Проверять, что все required поля перечислены в `inputSchema.required`

### 3. Создание детальных сообщений об ошибках

- [ ] Реализовать информативные сообщения при несоответствии:
  ```
  Schema-Definition mismatch in 'get_queue_fields':
  - Missing in definition.properties: fields
  - Missing in definition.required: ['fields']
  - Extra in definition (not in schema): []
  ```
- [ ] Добавить рекомендации по исправлению в сообщение об ошибке

### 4. Создание дополнительных тестов для конкретных случаев

- [ ] Создать тесты для 4 исправленных инструментов (regression tests):
  - `get-queue-fields`
  - `create-queue`
  - `update-queue`
  - `manage-queue-access`
- [ ] Добавить тесты для edge cases:
  - Инструмент без обязательных параметров
  - Инструмент с вложенными объектами
  - Инструмент с enum параметрами

### 5. Интеграция в существующую тестовую инфраструктуру

- [ ] Убедиться, что новые тесты запускаются командой `npm test`
- [ ] Убедиться, что новые тесты включены в `npm run validate`
- [ ] Проверить, что тесты работают в CI/CD pipeline

### 6. Документирование утилиты валидации

- [ ] Добавить JSDoc комментарии к функции валидатора
- [ ] Создать примеры использования в комментариях
- [ ] Добавить описание в `tests/README.md` если необходимо

---

## 🏗️ Архитектурные решения

### Подход к извлечению информации из Zod schema

**Вариант 1: Использование `schema.shape`**
```typescript
// Доступ к полям через shape
const schemaFields = Object.keys(schema.shape);
const requiredFields = schemaFields.filter(key =>
  !schema.shape[key].isOptional()
);
```

**Вариант 2: Использование `schema._def`**
- Более низкоуровневый доступ к структуре Zod
- Может быть нестабильным между версиями Zod

**Рекомендация:** Использовать вариант 1 (через `shape`)

### Подход к динамическому импорту инструментов

**Вариант 1: Использование glob + dynamic import**
```typescript
const toolFiles = glob.sync('src/tools/api/**/*.tool.ts');
for (const file of toolFiles) {
  const module = await import(file);
  // ...
}
```

**Вариант 2: Явный список инструментов**
- Менее автоматизирован
- Более предсказуем

**Рекомендация:** Использовать вариант 1 для автоматизации

---

## 📝 Выходные артефакты

1. **Файл:** `tests/helpers/schema-definition-validator.ts`
   - Утилита для валидации соответствия schema-definition

2. **Файл:** `tests/tools/schema-definition-consistency.test.ts`
   - Автоматические тесты для всех инструментов

3. **Обновление:** `tests/README.md` (если необходимо)
   - Документация новых тестов и утилит

---

## ⚠️ Критерии завершения

- [ ] Утилита валидации создана и покрыта тестами
- [ ] Автоматический тест для всех инструментов создан
- [ ] Все существующие инструменты проходят валидацию
- [ ] Тесты для 4 исправленных инструментов добавлены (regression)
- [ ] Тесты интегрированы в `npm run validate`
- [ ] Запущена валидация: `npm run validate` — успешно
- [ ] Создан коммит и пуш в ветку плана
- [ ] Согласовано с пользователем

---

## 🔗 Связанные этапы

- **Зависит от:** 0.1 (анализ)
- **Параллельно с:** 1.2, 1.3, 1.4
- **Блокирует:** 2.1 (интеграция)
