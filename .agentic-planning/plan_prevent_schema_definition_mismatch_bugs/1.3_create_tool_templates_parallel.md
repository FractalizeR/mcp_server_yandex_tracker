# Этап 1.3: Создание шаблонов и генераторов инструментов (Parallel)

**Приоритет:** Средний
**Тип выполнения:** Parallel (можно выполнять параллельно с этапами 1.1, 1.2, 1.4)
**Время:** 1-2 часа
**Зависимости:** Требует завершения этапа 0.1

---

## 🎯 Цель

Создать шаблоны и/или генератор для новых инструментов, которые автоматически включают все необходимые компоненты (включая `buildFieldsParam()`) и следуют best practices. Это предотвратит баги на этапе создания новых инструментов.

---

## ✅ Чек-лист задач

### 1. Анализ существующих паттернов создания инструментов

- [ ] Изучить структуру 5-10 существующих инструментов
- [ ] Выявить общие паттерны:
  - Структура файлов (tool, schema, definition, metadata)
  - Обязательные параметры (fields, issueId и т.д.)
  - Паттерны именования
  - Структура тестов
- [ ] Определить, что должно быть в шаблоне

### 2. Создание шаблонов файлов

- [ ] Создать директорию `templates/tool/` в корне проекта
- [ ] Создать файл `__TOOL_NAME__.tool.ts.template`:
  - Класс с extends BaseTool
  - Метод execute() с TODO
  - Импорты из правильных мест
  - Плейсхолдеры для замены: `__TOOL_NAME__`, `__CATEGORY__`, `__DESCRIPTION__`
- [ ] Создать файл `__TOOL_NAME__.schema.ts.template`:
  - Zod схема с базовыми полями
  - Обязательный `fields: FieldsSchema`
  - Export типа
- [ ] Создать файл `__TOOL_NAME__.definition.ts.template`:
  - Класс с extends BaseToolDefinition
  - Метод build() с базовой структурой
  - Обязательный `fields: this.buildFieldsParam()` в properties
  - Обязательный `'fields'` в required
  - Метод `buildFieldsParam()` уже реализован
- [ ] Создать файл `__TOOL_NAME__.metadata.ts.template`:
  - Экспорт метаданных с плейсхолдерами

### 3. Создание генератора (CLI скрипт)

- [ ] Создать файл `scripts/generate-tool.ts`
- [ ] Реализовать интерактивный промпт для сбора информации:
  - Имя инструмента (например: `create-issue`)
  - Категория (issues, queues, comments и т.д.)
  - Описание инструмента
  - Требуется ли fields параметр (по умолчанию: да)
  - Дополнительные параметры (интерактивный ввод)
- [ ] Реализовать генерацию файлов из шаблонов:
  - Читать template файлы
  - Заменять плейсхолдеры на реальные значения
  - Создавать файлы в правильной директории
  - Генерировать базовый тест
- [ ] Реализовать post-generation шаги:
  - Запустить prettier на сгенерированных файлах
  - Запустить eslint --fix
  - Вывести инструкции следующих шагов

### 4. Добавление npm скрипта для генератора

- [ ] Добавить в `package.json`:
  ```json
  "scripts": {
    "generate:tool": "tsx scripts/generate-tool.ts"
  }
  ```
- [ ] Добавить описание скрипта в package.json (если есть секция description)

### 5. Создание документации по использованию

- [ ] Создать файл `templates/tool/README.md`:
  - Инструкция по запуску генератора
  - Примеры использования
  - Что делать после генерации
  - Best practices
- [ ] Обновить основной README.md:
  - Добавить раздел "Creating new tools"
  - Ссылка на шаблоны и генератор

### 6. Создание чеклиста для ручного создания инструментов

- [ ] Если генератор не используется, создать чеклист в документации:
  ```markdown
  ## Чеклист создания нового инструмента

  - [ ] Создан файл `*.tool.ts`
  - [ ] Создан файл `*.schema.ts` с `fields: FieldsSchema`
  - [ ] Создан файл `*.definition.ts`
  - [ ] В definition добавлен `fields: this.buildFieldsParam()` в properties
  - [ ] В definition добавлен `'fields'` в required[]
  - [ ] Реализован метод `buildFieldsParam()`
  - [ ] Создан unit тест
  - [ ] Запущена валидация: `npm run validate`
  ```

### 7. Тестирование генератора

- [ ] Сгенерировать тестовый инструмент с помощью генератора
- [ ] Проверить, что все файлы созданы корректно
- [ ] Проверить, что generated код проходит lint
- [ ] Проверить, что generated код проходит typecheck
- [ ] Запустить валидацию schema-definition на generated инструменте
- [ ] Удалить тестовый инструмент после проверки

---

## 🏗️ Архитектурные решения

### Выбор инструмента для генератора

**Вариант 1: Plop.js**
- Популярный генератор на основе шаблонов
- Интерактивные промпты
- Требует дополнительную зависимость

**Вариант 2: Кастомный скрипт на TypeScript**
- Полный контроль
- Нет зависимостей
- Требует больше кода

**Вариант 3: Yeoman**
- Мощный, но тяжеловесный
- Избыточен для наших нужд

**Рекомендация:** Использовать вариант 2 (кастомный скрипт) для простоты

### Подход к плейсхолдерам

**Вариант 1: Mustache/Handlebars стиль**
```typescript
{{TOOL_NAME}}
{{CATEGORY}}
```

**Вариант 2: Простая замена строк**
```typescript
__TOOL_NAME__
__CATEGORY__
```

**Рекомендация:** Использовать вариант 2 для простоты

---

## 📝 Выходные артефакты

1. **Директория:** `templates/tool/`
   - Шаблоны для всех файлов инструмента

2. **Файл:** `scripts/generate-tool.ts`
   - CLI генератор инструментов

3. **Обновление:** `package.json`
   - npm скрипт `generate:tool`

4. **Файл:** `templates/tool/README.md`
   - Документация по использованию генератора

5. **Обновление:** Основной `README.md`
   - Раздел о создании новых инструментов

---

## ⚠️ Критерии завершения

- [ ] Шаблоны созданы для всех типов файлов
- [ ] Генератор работает и создает корректные файлы
- [ ] Generated код проходит lint и typecheck
- [ ] Generated код проходит schema-definition валидацию
- [ ] Документация обновлена
- [ ] Чеклист для ручного создания добавлен
- [ ] Запущена валидация: `npm run validate` — успешно
- [ ] Создан коммит и пуш в ветку плана
- [ ] Согласовано с пользователем

---

## 🔗 Связанные этапы

- **Зависит от:** 0.1 (анализ)
- **Параллельно с:** 1.1, 1.2, 1.4
- **Блокирует:** 2.1 (интеграция)
