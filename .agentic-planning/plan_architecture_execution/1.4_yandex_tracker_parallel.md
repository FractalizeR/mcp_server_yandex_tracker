# Этап 1.4: Yandex-Tracker Improvements (PARALLEL)

**Время:** 5-7 часов
**Тип:** PARALLEL (Agent 3)
**Ветка:** `claude/execution-1.4-yandex-tracker-{session_id}`

## 🎯 Цель

DRY в CLI, DI validation

**Примечание:** Entity Factories review перенесен в Этап 3.1 (после Facade рефакторинга)

## 📋 Задачи

### Task 1: CLI BaseConnector (4-6ч)

**Цель:** DRY для 5 connectors (OAuth, IAM, Organization, etc.)

**Шаги:**
1. Создать `BaseConnector<TConfig>` abstract class (1ч)
2. Refactor 5 connectors → extend BaseConnector (2-3ч)
3. Тесты (1ч)

**Пример BaseConnector:**
```typescript
// src/cli/connectors/base-connector.ts
export abstract class BaseConnector<TConfig> {
  // Abstract (каждый connector реализует):
  abstract getConfigPath(): string;
  abstract getDefaultConfig(): TConfig;
  abstract getName(): string;

  // Common (переиспользуются):
  async readConfig(): Promise<TConfig> {
    const path = this.getConfigPath();
    if (!existsSync(path)) return this.getDefaultConfig();
    return JSON.parse(await readFile(path, 'utf-8'));
  }

  async writeConfig(config: TConfig): Promise<void> {
    await writeFile(this.getConfigPath(), JSON.stringify(config, null, 2));
  }
}
```

**Пример использования:**
```typescript
export class OAuthConnector extends BaseConnector<OAuthConfig> {
  getConfigPath() { return '~/.yandex-tracker/oauth.json'; }
  getDefaultConfig() { return { token: null, orgId: null }; }
  getName() { return 'OAuth'; }
  // readConfig/writeConfig наследуются!
}
```

**Результат:** 750 → 350 строк (-53%)

### Task 2: DI Validation (1ч)
- [ ] Add validation в bindOperations
- [ ] Add validation в bindTools
- [ ] Throw clear errors
- [ ] Тесты

## ✅ Success Criteria
- [ ] Duplication reduced by >50% (CLI)
- [ ] DI validation works
- [ ] All tests pass
- [ ] Коммит и пуш
