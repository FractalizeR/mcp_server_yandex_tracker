# Boundary Violations Analysis

**Дата:** 2025-11-21
**Автор:** Claude Code

---

## 🎯 Цель

Проверка соблюдения архитектурных границ:
1. Отсутствие утечки доменной логики в framework пакеты
2. Правильное использование Facade (нет bypass)
3. Использование DI (нет прямых `new`)

---

## 🔴 Критические нарушения

### 1. Утечка доменной логики в Infrastructure

**Файлы с проблемами:**
- `packages/framework/infrastructure/src/constants.ts`
- `packages/framework/infrastructure/src/config.ts`
- `packages/framework/infrastructure/src/types.ts`
- `packages/framework/infrastructure/src/http/client/http-config.interface.ts`

**Примеры нарушений:**

#### constants.ts (строки 8, 23-26)
```typescript
// 🔴 ПРОБЛЕМА: Доменные константы в framework пакете
export const DEFAULT_API_BASE = 'https://api.tracker.yandex.net' as const;

export const ENV_VAR_NAMES = {
  YANDEX_TRACKER_TOKEN: 'YANDEX_TRACKER_TOKEN',
  YANDEX_ORG_ID: 'YANDEX_ORG_ID',
  YANDEX_CLOUD_ORG_ID: 'YANDEX_CLOUD_ORG_ID',
  YANDEX_TRACKER_API_BASE: 'YANDEX_TRACKER_API_BASE',
  // ... остальные константы
};
```

#### config.ts
```typescript
// 🔴 ПРОБЛЕМА: Доменная логика валидации в framework
const token = process.env[ENV_VAR_NAMES.YANDEX_TRACKER_TOKEN];
if (!token || token.trim() === '') {
  throw new Error(
    `${ENV_VAR_NAMES.YANDEX_TRACKER_TOKEN} не установлен. ` +
    'Получите OAuth токен: https://oauth.yandex.ru/...'
  );
}
```

#### types.ts
```typescript
// 🔴 ПРОБЛЕМА: Доменные типы в framework
export interface ServerConfig {
  /** ID организации (Yandex Cloud Organization) */
  organizationId: string;
  // ...
}
```

---

## 📊 Оценка серьезности

### Нарушенные принципы:

| Принцип | Нарушение | Серьезность |
|---------|-----------|-------------|
| **SRP** | Infrastructure отвечает за Yandex Tracker конфигурацию | 🔴 CRITICAL |
| **OCP** | Нельзя переиспользовать infrastructure для других проектов | 🔴 CRITICAL |
| **DIP** | Infrastructure зависит от доменных понятий (Yandex Tracker) | 🔴 CRITICAL |
| **ISP** | ServerConfig содержит специфичные для Yandex поля | 🟡 IMPORTANT |

---

## ⚠️ Влияние на архитектуру

### Проблемы:

1. **Невозможность переиспользования:**
   - Infrastructure не может быть использован в других проектах
   - Нарушает цель создания framework пакетов

2. **Coupling с доменом:**
   - Infrastructure "знает" о Yandex Tracker
   - Создает обратную зависимость (framework → domain)

3. **Нарушение layered architecture:**
   - Низкий слой (infrastructure) зависит от высокого (domain)
   - Граф зависимостей формально соблюден, но семантически нарушен

4. **Усложнение тестирования:**
   - Infrastructure tests зависят от доменных константов
   - Невозможно тестировать независимо

---

## ✅ Нарушений НЕ найдено

### 2. Bypass Facade

**Проверка:** tools не используют HTTP напрямую

```bash
grep -r "axios\|fetch\|http\." packages/servers/yandex-tracker/src/tools/ --include="*.ts"
```

**Результат:**
```
packages/servers/yandex-tracker/src/tools/api/issues/get/get-issues.metadata.ts:  tags: ['read', 'get', 'fetch', 'issue'],
```

**Оценка:** ✅ PASS - "fetch" это тег в metadata, не HTTP вызов

---

### 3. Bypass DI

**Проверка:** tools не создают Operations/Facade напрямую

```bash
grep -r "new.*Operation\|new.*Facade" packages/servers/yandex-tracker/src/tools/ --include="*.ts"
```

**Результат:** Нет совпадений

**Оценка:** ✅ PASS - Используется DI для всех зависимостей

---

## 📋 Детальный анализ утечки домена

### Файлы с упоминаниями "yandex" или "tracker":

| Файл | Тип | Серьезность |
|------|-----|-------------|
| `infrastructure/src/constants.ts` | Доменные константы | 🔴 CRITICAL |
| `infrastructure/src/config.ts` | Доменная валидация | 🔴 CRITICAL |
| `infrastructure/src/types.ts` | Доменные комментарии | 🟡 IMPORTANT |
| `infrastructure/src/http/client/http-config.interface.ts` | Доменные примеры | 🟡 IMPORTANT |
| `core/src/tools/base/base-definition.ts` | Примеры в комментариях | 🟢 LOW |
| `core/src/tools/common/utils/tool-name.ts` | Примеры в комментариях | 🟢 LOW |
| `search/src/tools/search-tools.tool.ts` | Комментарии | 🟢 LOW |

---

## 💡 Рекомендации

### 🔴 Критические (требуют немедленного исправления)

#### 1. Вынести доменные константы из infrastructure

**Проблема:**
- `DEFAULT_API_BASE`, `ENV_VAR_NAMES` с Yandex Tracker специфичными значениями

**Решение:**
1. Создать `packages/servers/yandex-tracker/src/constants.ts`
2. Переместить туда:
   - `DEFAULT_API_BASE = 'https://api.tracker.yandex.net'`
   - `YANDEX_TRACKER_TOKEN`, `YANDEX_ORG_ID`, etc.

3. В infrastructure оставить только generic константы:
   - `DEFAULT_LOG_LEVEL`
   - `DEFAULT_REQUEST_TIMEOUT`
   - `DEFAULT_MAX_BATCH_SIZE`
   - etc.

**Effort:** MEDIUM (~3-4 часа)
**Impact:** HIGH (делает infrastructure переиспользуемым)
**Breaking Change:** ⚠️ YES (изменение экспортов)

---

#### 2. Вынести доменную валидацию из infrastructure/config.ts

**Проблема:**
- `loadConfig()` содержит специфичную для Yandex Tracker логику

**Решение:**
1. Создать generic `loadBaseConfig()` в infrastructure:
   ```typescript
   export function loadBaseConfig(envVars: Record<string, string>): BaseConfig {
     // Generic валидация (timeout, batch size, etc.)
   }
   ```

2. Создать `loadYandexTrackerConfig()` в yandex-tracker:
   ```typescript
   export function loadYandexTrackerConfig(): YandexTrackerConfig {
     const base = loadBaseConfig(process.env);
     // Yandex-specific валидация
     const token = process.env.YANDEX_TRACKER_TOKEN;
     // ...
   }
   ```

**Effort:** MEDIUM (~4-5 часов)
**Impact:** HIGH (разделение concerns)
**Breaking Change:** ⚠️ YES (изменение API)

---

#### 3. Сделать ServerConfig generic

**Проблема:**
- `ServerConfig` содержит `organizationId` (Yandex-specific)

**Решение:**
1. В infrastructure создать `BaseServerConfig`:
   ```typescript
   export interface BaseServerConfig {
     apiBase: string;
     token: string;
     timeout: number;
     // ... generic поля
   }
   ```

2. В yandex-tracker расширить:
   ```typescript
   export interface YandexTrackerServerConfig extends BaseServerConfig {
     organizationId: string;
     cloudOrganizationId?: string;
   }
   ```

**Effort:** LOW (~2 часа)
**Impact:** MEDIUM (улучшает generic design)
**Breaking Change:** ⚠️ YES (изменение типов)

---

### 🟡 Важные (желательно исправить)

#### 4. Удалить доменные комментарии/примеры

**Проблема:**
- Комментарии и примеры упоминают Yandex Tracker

**Решение:**
- Заменить на generic примеры:
  ```typescript
  // Было:
  /** Базовый URL API (например, 'https://api.tracker.yandex.net') */

  // Стало:
  /** Базовый URL API (например, 'https://api.example.com') */
  ```

**Effort:** LOW (~1 час)
**Impact:** LOW (косметическое улучшение)
**Breaking Change:** ❌ NO

---

## 📊 План миграции

### Phase 1: Подготовка (не ломает код)

1. ✅ Создать новые файлы в yandex-tracker:
   - `src/config/yandex-tracker.constants.ts`
   - `src/config/yandex-tracker.config.ts`
   - `src/config/yandex-tracker.types.ts`

2. ✅ Скопировать туда доменные части

3. ✅ Создать generic версии в infrastructure

### Phase 2: Migration (breaking changes)

4. ⚠️ Обновить импорты в yandex-tracker
5. ⚠️ Удалить доменные части из infrastructure
6. ⚠️ Обновить tests

### Phase 3: Cleanup

7. ✅ Обновить комментарии/примеры
8. ✅ Обновить документацию
9. ✅ Добавить архитектурные тесты для предотвращения regression

---

## 📊 Сравнение с baseline

| Метрика | Целевое значение | Фактическое | Статус |
|---------|------------------|-------------|--------|
| Утечек домена в infrastructure | 0 | 4 файла | 🔴 FAIL |
| Утечек домена в core | 0 | 0 (только комментарии) | ✅ PASS |
| Утечек домена в search | 0 | 0 (только комментарии) | ✅ PASS |
| Bypass Facade | 0 | 0 | ✅ PASS |
| Bypass DI | 0 | 0 | ✅ PASS |

---

## 💡 Выводы

### 🔴 Критические проблемы

1. **Infrastructure содержит доменную логику Yandex Tracker**
   - Нарушает SRP, OCP, DIP
   - Делает infrastructure не переиспользуемым
   - Требует рефакторинга для извлечения доменных частей

### ✅ Сильные стороны

1. **Правильное использование Facade:** tools не обращаются к HTTP напрямую
2. **Правильное использование DI:** нет прямого создания зависимостей
3. **Изоляция слоев:** tracker_api не импортирует MCP

---

## 📋 Следующие шаги

- [x] Проверка утечки домена
- [x] Проверка bypass Facade
- [x] Проверка bypass DI
- [x] Создание рекомендаций
- [x] Создание плана миграции
- [ ] Переход к созданию summary отчета (следующая задача)

---

## 🔗 Связанные документы

- См. также: `1.2_monorepo_dependencies.md` - Граф зависимостей между пакетами
- См. также: `1.2_internal_layering.md` - Внутренняя слоистость yandex-tracker
