# Этап 4.1: Attachment tools error paths (Parallel)

**Цель:** Покрыть error handling при сохранении файлов в attachment tools (74-78% → 85%+)

**Тип выполнения:** Parallel (можно выполнять одновременно с 4.2 и 4.3)

**Файлы для тестирования:**
- `src/tools/api/issues/attachments/download/download-attachment.tool.ts` (77.77%)
- `src/tools/api/issues/attachments/thumbnail/get-thumbnail.tool.ts` (77.77%)
- `src/tools/api/issues/attachments/delete/delete-attachment.tool.ts` (73.91%)

**Редактируемые файлы:**
- `tests/integration/tools/api/issues/attachments/download/download-attachment.tool.integration.test.ts`
- `tests/integration/tools/api/issues/attachments/thumbnail/get-thumbnail.tool.integration.test.ts`
- `tests/integration/tools/api/issues/attachments/delete/delete-attachment.tool.integration.test.ts`

**Оценка времени:** 1 час

**Приоритет:** 🟢 ОПЦИОНАЛЬНО

---

## Чек-лист задач

### 1. Подготовка

- [ ] Прочитать исходный код трех tools:
  - download-attachment.tool.ts (строки 68-72 не покрыты)
  - get-thumbnail.tool.ts (строки 67-71 не покрыты)
  - delete-attachment.tool.ts (строки 64-74 не покрыты)
- [ ] Определить общий паттерн непокрытого кода:
  ```typescript
  if (saveToPath && result.content instanceof Buffer) {
    try {
      await writeFile(saveToPath, result.content);
      this.logger.info(`...`);
    } catch (error) {
      return this.formatError(`...`, error as Error);
    }
  }
  ```
- [ ] Изучить существующие интеграционные тесты для этих tools

### 2. Download Attachment Tool — error paths

**Файл:** `tests/integration/tools/api/issues/attachments/download/download-attachment.tool.integration.test.ts`

- [ ] Найти существующие тесты для `saveToPath` параметра
- [ ] Добавить новый `describe('Error Handling при сохранении файла')`
- [ ] Тест: ошибка при записи файла (недоступная директория)
  - Подготовить: мок для успешного скачивания attachment
  - Указать `saveToPath` в недоступную директорию (например, `/root/protected/file.txt`)
  - Ожидаемое поведение: tool возвращает error через `formatError`
  - Проверить: сообщение об ошибке содержит путь
- [ ] Тест: ошибка переполнения диска (если можно симулировать)
  - Альтернатива: использовать мок `writeFile` с ошибкой ENOSPC
- [ ] Тест: ошибка при невалидном пути
  - Указать `saveToPath` с невалидными символами или null bytes
  - Ожидаемое поведение: error через `formatError`

### 3. Get Thumbnail Tool — error paths

**Файл:** `tests/integration/tools/api/issues/attachments/thumbnail/get-thumbnail.tool.integration.test.ts`

- [ ] Найти существующие тесты для `saveToPath` параметра
- [ ] Добавить новый `describe('Error Handling при сохранении миниатюры')`
- [ ] Тест: ошибка при записи файла (недоступная директория)
  - Подготовить: мок для успешного получения thumbnail
  - Указать `saveToPath` в недоступную директорию
  - Ожидаемое поведение: tool возвращает error через `formatError`
  - Проверить: сообщение содержит "Не удалось сохранить миниатюру"
- [ ] Тест: ошибка при нехватке прав на запись
  - Использовать директорию с правами read-only (если возможно в тестовом окружении)
  - Альтернатива: мок `writeFile` с ошибкой EACCES

### 4. Delete Attachment Tool — error paths

**Файл:** `tests/integration/tools/api/issues/attachments/delete/delete-attachment.tool.integration.test.ts`

**Примечание:** В delete-attachment.tool.ts есть блок сохранения в файл, но он не должен использоваться для delete операции. Возможно, это копипаста из других tools.

- [ ] Проверить, используется ли `saveToPath` в delete операции
- [ ] Если используется (что странно для delete):
  - Добавить тесты по аналогии с download/thumbnail
- [ ] Если НЕ используется:
  - Добавить комментарий в тесте: "saveToPath не используется в delete операции"
  - Рассмотреть возможность удаления этого кода из tool (создать issue)

### 5. Альтернативный подход: моки fs.promises.writeFile

**Если доступ к недоступным директориям проблематичен в тестовом окружении:**

- [ ] Использовать `vi.spyOn(fs.promises, 'writeFile')`
- [ ] В тестах настроить мок на выброс ошибки:
  ```typescript
  vi.spyOn(fs.promises, 'writeFile').mockRejectedValueOnce(
    new Error('EACCES: permission denied')
  );
  ```
- [ ] Проверить, что tool корректно обрабатывает ошибку
- [ ] Не забыть очистить моки в `afterEach`

### 6. Проверка логирования ошибок

- [ ] В каждом error path тесте проверить:
  - Tool возвращает объект error (не выбрасывает исключение)
  - Сообщение об ошибке понятное и содержит контекст (путь к файлу)
  - Logger вызван с уровнем error (опционально, если есть доступ к логам)

### 7. Валидация и финализация

- [ ] Запустить интеграционные тесты для attachment tools:
  - `npm test download-attachment.tool.integration.test.ts`
  - `npm test get-thumbnail.tool.integration.test.ts`
  - `npm test delete-attachment.tool.integration.test.ts`
- [ ] Проверить покрытие для каждого tool: должно быть ≥ 85%
- [ ] Убедиться, что непокрытые строки (68-72, 67-71, 64-74) теперь покрыты
- [ ] Исправить failing тесты
- [ ] Проверить ESLint: `npm run lint`
- [ ] Проверить TypeScript: `npm run typecheck`
- [ ] Проверить cleanup (если использовались моки fs)

---

## Архитектурные требования

### Интеграционные тесты
- Это интеграционные тесты, поэтому должны использовать реальный file system (в temp директории)
- Альтернатива: моки для fs.promises.writeFile, если реальные FS операции проблематичны

### Изоляция
- Использовать временные директории для тестирования
- Очищать temp файлы в `afterEach`
- Не оставлять артефакты после тестов

### Error Handling
- Проверять, что tools НЕ выбрасывают исключения
- Проверять, что errors возвращаются через `formatError` метод
- Проверять структуру error объекта (isError: true, content с описанием)

---

## Критерии готовности

✅ Добавлено ~6 тест-кейсов (по 2 на каждый tool)
✅ Покрытие download-attachment.tool.ts ≥ 85%
✅ Покрытие get-thumbnail.tool.ts ≥ 85%
✅ Покрытие delete-attachment.tool.ts ≥ 85% (или пояснено, почему код не используется)
✅ Нет ESLint/TypeScript ошибок
✅ Временные файлы корректно очищаются

---

## Следующий шаг

После завершения:
1. Коммит: `chore: добавить error path тесты для attachment tools`
2. Пуш в ветку
3. Опционально: создать issue для удаления неиспользуемого кода в delete-attachment.tool.ts
