# Этап 4.3: Исправление search integration теста (Parallel) ✅

**Статус:** ВЫПОЛНЕНО (2025-11-20)

**Цель:** Исправить падающий integration тест в @mcp-framework/search

**Тип выполнения:** Parallel (можно выполнять одновременно с 4.1 и 4.2)

**Пакет:** `@mcp-framework/search`

**Проблемный файл:** `packages/framework/search/tests/integration/tool-search-engine.test.ts`

**Ошибка:**
```
Cannot find package '@mcp-framework/search/tool-search-engine.js'
```

**Оценка времени:** 30 минут

**Приоритет:** 🟢 ОПЦИОНАЛЬНО

---

## Чек-лист задач

### 1. Диагностика проблемы

- [ ] Прочитать файл `packages/framework/search/tests/integration/tool-search-engine.test.ts`
- [ ] Изучить строки с ошибками (около строки 13):
  ```typescript
  import { ToolSearchEngine } from '@mcp-framework/search/tool-search-engine.js';
  import { WeightedCombinedStrategy } from '@mcp-framework/search/strategies/...';
  ```
- [ ] Проверить, существуют ли эти файлы:
  - `packages/framework/search/src/tool-search-engine.ts`
  - `packages/framework/search/src/strategies/...`
- [ ] Проверить `package.json` в search пакете:
  - Есть ли exports для `tool-search-engine.js`?
  - Есть ли exports для `strategies/*`?

### 2. Проверка exports в package.json

- [ ] Открыть `packages/framework/search/package.json`
- [ ] Найти секцию `exports`:
  ```json
  "exports": {
    ".": "./dist/index.js",
    "./tool-search-engine": "./dist/tool-search-engine.js",
    "./strategies/*": "./dist/strategies/*.js"
  }
  ```
- [ ] Проверить, что все пути существуют в `dist/` после сборки
- [ ] Если exports отсутствуют или неверны — исправить

### 3. Вариант A: Исправление imports в тесте

**Если exports корректны, но imports неверные:**

- [ ] Изменить импорты на корректные:
  ```typescript
  // Было:
  import { ToolSearchEngine } from '@mcp-framework/search/tool-search-engine.js';

  // Должно быть:
  import { ToolSearchEngine } from '@mcp-framework/search';
  // или
  import { ToolSearchEngine } from '@mcp-framework/search/tool-search-engine';
  ```
- [ ] Изменить импорты для strategies:
  ```typescript
  // Было:
  import { WeightedCombinedStrategy } from '@mcp-framework/search/strategies/weighted-combined.strategy.js';

  // Должно быть:
  import { WeightedCombinedStrategy } from '@mcp-framework/search';
  // или
  import { WeightedCombinedStrategy } from '@mcp-framework/search/strategies/weighted-combined.strategy';
  ```

### 4. Вариант B: Добавление exports в package.json

**Если exports отсутствуют:**

- [ ] Открыть `packages/framework/search/package.json`
- [ ] Добавить или обновить секцию `exports`:
  ```json
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    },
    "./tool-search-engine": {
      "types": "./dist/tool-search-engine.d.ts",
      "import": "./dist/tool-search-engine.js"
    },
    "./strategies/*": {
      "types": "./dist/strategies/*.d.ts",
      "import": "./dist/strategies/*.js"
    }
  }
  ```
- [ ] Убедиться, что пути соответствуют структуре `dist/`

### 5. Вариант C: Использование относительных imports

**Альтернативный подход (менее предпочтительный):**

- [ ] Изменить импорты на относительные:
  ```typescript
  import { ToolSearchEngine } from '../../src/tool-search-engine.js';
  import { WeightedCombinedStrategy } from '../../src/strategies/weighted-combined.strategy.js';
  ```
- [ ] **Примечание:** Это нарушает конвенцию использования package names, но гарантирует работоспособность

### 6. Проверка vitest конфигурации

**Возможно, проблема в vitest workspace mode:**

- [ ] Открыть `vitest.workspace.ts` в корне monorepo
- [ ] Проверить, что search пакет включен в workspace
- [ ] Проверить, есть ли специфичные настройки для search:
  ```typescript
  {
    test: {
      name: 'search',
      root: './packages/framework/search',
      // ...
    }
  }
  ```
- [ ] Если используется `vitest.config.ts` в search пакете — проверить настройки resolve

### 7. Проверка сборки пакета

**Проблема может быть в несобранном пакете:**

- [ ] Собрать search пакет: `npm run build --workspace=@mcp-framework/search`
- [ ] Проверить, что `dist/` создан и содержит нужные файлы:
  - `dist/tool-search-engine.js`
  - `dist/strategies/weighted-combined.strategy.js`
- [ ] Если файлы отсутствуют — проверить `tsconfig.json` и настройки сборки

### 8. Тестирование исправления

- [ ] Запустить тест: `npm test --workspace=@mcp-framework/search`
- [ ] Проверить, что ошибка импорта исчезла
- [ ] Если тест все еще падает — изучить новую ошибку
- [ ] Исправить дополнительные проблемы (если есть)

### 9. Валидация и финализация

- [ ] Запустить все тесты search пакета: `npm test --workspace=@mcp-framework/search`
- [ ] Проверить, что integration тест проходит
- [ ] Проверить coverage: должно быть согласно целям пакета
- [ ] Проверить ESLint: `npm run lint --workspace=@mcp-framework/search`
- [ ] Проверить TypeScript: `npm run typecheck --workspace=@mcp-framework/search`
- [ ] Запустить тесты всего monorepo: `npm test` — убедиться, что ничего не сломалось

---

## Архитектурные требования

### Package exports
- Предпочтительно использовать package name imports: `@mcp-framework/search`
- Wildcard exports (`strategies/*`) должны работать корректно
- Все exports должны указывать на скомпилированные файлы в `dist/`

### Vitest workspace mode
- Integration тесты должны корректно резолвить path aliases в workspace mode
- Если проблема в workspace mode — рассмотреть запуск через `--workspaces` (уже используется)

### TypeScript paths
- Проверить, что TypeScript paths в `tsconfig.json` корректны
- Убедиться, что `tsc-alias` корректно обрабатывает пути при сборке

---

## Критерии готовности

✅ Ошибка импорта устранена
✅ Integration тест `tool-search-engine.test.ts` проходит
✅ Все тесты search пакета проходят
✅ Coverage search пакета не снизился
✅ Нет ESLint/TypeScript ошибок
✅ Не сломаны тесты других пакетов

---

## Следующий шаг

После завершения:
1. Коммит:
   - Если изменены imports: `fix: исправить imports в search integration тесте`
   - Если изменен package.json: `fix: добавить exports для tool-search-engine в @mcp-framework/search`
   - Если оба: `fix: исправить path resolution для search integration теста`
2. Пуш в ветку
3. Проверить CI/CD — должны проходить все тесты

---

## ✅ Результаты выполнения

**Дата:** 2025-11-20
**Действия:** Сборка пакета (без изменений кода)
**Ветка:** `claude/stage-4-test-coverage-01FZoV2iguG2GwiZjicjCyNR`

### Проблема:

```
Cannot find package '@mcp-framework/search/tool-search-engine.js'
```

**Причина:** Пакет `@mcp-framework/search` не был собран - отсутствовала директория `dist/` с скомпилированными файлами.

### Решение:

```bash
npm run build --workspace=@mcp-framework/search
```

Сборка создала:
- `dist/engine/tool-search-engine.js`
- `dist/strategies/*.js`
- Все необходимые `.d.ts` файлы

### Результат:

- ✅ Все 135 тестов пакета `@mcp-framework/search` проходят успешно
- ✅ Integration тест `tool-search-engine.test.ts` работает корректно
- ✅ Не потребовалось изменений в коде или конфигурации
- ✅ `package.json` exports настроены правильно

### Техническая деталь:

Exports в `package.json` пакета search уже были корректно настроены:
```json
{
  "./tool-search-engine.js": {
    "types": "./dist/engine/tool-search-engine.d.ts",
    "default": "./dist/engine/tool-search-engine.js"
  }
}
```

Проблема была только в отсутствии собранных файлов, а не в конфигурации.
