# Этап 3.2: Facade методы (worklog/fields/boards/sprints) (Parallel)

**Цель:** Покрыть тестами непокрытые методы YandexTrackerFacade (59% → 80%+)

**Тип выполнения:** Parallel (можно выполнять одновременно с 3.1 и 3.3)

**Файл для тестирования:** `packages/servers/yandex-tracker/src/tracker_api/facade/yandex-tracker.facade.ts`

**Редактируемый файл:** `packages/servers/yandex-tracker/tests/tracker_api/facade/yandex-tracker.facade.test.ts` (существует)

**Оценка времени:** 1-2 часа

**Приоритет:** 🟡 ЖЕЛАТЕЛЬНО

---

## Чек-лист задач

### 1. Подготовка

- [ ] Прочитать существующий тестовый файл `tests/tracker_api/facade/yandex-tracker.facade.test.ts`
- [ ] Изучить паттерн существующих тестов:
  - Использование моков для operations
  - Структура тестов (должны проверять делегирование, не бизнес-логику)
- [ ] Прочитать исходный код facade, непокрытые строки: 593-654, 793-1078
- [ ] Определить, какие методы уже покрыты

### 2. Непокрытые методы: Worklog (строки 602-655)

**Цель:** Проверить делегирование к worklog operations

- [ ] **describe('Worklog methods'):**
  - [ ] Тест: `getWorklogs(issueId)`
    - Создать мок для `GetWorklogsOperation`
    - Вызвать `facade.getWorklogs('TEST-1')`
    - Проверить: operation вызван с правильными параметрами
    - Проверить: результат operation возвращен
  - [ ] Тест: `addWorklog(issueId, input)`
    - Создать мок для `AddWorklogOperation`
    - Подготовить input: `{ duration: 'PT1H', comment: 'Work done' }`
    - Вызвать `facade.addWorklog('TEST-1', input)`
    - Проверить: operation вызван с правильными параметрами
    - Проверить: результат возвращен
  - [ ] Тест: `updateWorklog(issueId, worklogId, input)`
    - Создать мок для `UpdateWorklogOperation`
    - Вызвать `facade.updateWorklog('TEST-1', '123', { duration: 'PT2H' })`
    - Проверить: делегирование корректно
  - [ ] Тест: `deleteWorklog(issueId, worklogId)`
    - Создать мок для `DeleteWorklogOperation`
    - Вызвать `facade.deleteWorklog('TEST-1', '123')`
    - Проверить: operation вызван с правильными параметрами

### 3. Непокрытые методы: Fields (строки 909-962)

**Цель:** Проверить делегирование к field operations

- [ ] **describe('Field methods'):**
  - [ ] Тест: `getFields()`
    - Создать мок для `GetFieldsOperation` (если существует)
    - Вызвать `facade.getFields()`
    - Проверить: operation вызван
    - Проверить: результат (массив полей) возвращен
  - [ ] Тест: `getField(fieldId)`
    - Создать мок для `GetFieldOperation`
    - Вызвать `facade.getField('customField123')`
    - Проверить: operation вызван с правильным fieldId
  - [ ] Тест: `createField(input)`
    - Создать мок для `CreateFieldOperation`
    - Подготовить input: `{ name: 'Custom Field', type: 'string' }`
    - Вызвать `facade.createField(input)`
    - Проверить: делегирование корректно
  - [ ] Тест: `updateField(fieldId, input)`
    - Создать мок для `UpdateFieldOperation`
    - Вызвать `facade.updateField('customField123', { name: 'Updated' })`
    - Проверить: operation вызван с правильными параметрами
  - [ ] Тест: `deleteField(fieldId)`
    - Создать мок для `DeleteFieldOperation`
    - Вызвать `facade.deleteField('customField123')`
    - Проверить: operation вызван

**Примечание:** Если операции для Fields еще не реализованы, пропустить эти тесты или создать заглушки.

### 4. Непокрытые методы: Boards (строки 971-1025)

**Цель:** Проверить делегирование к board operations

- [ ] **describe('Board methods'):**
  - [ ] Тест: `getBoards(params?)`
    - Создать мок для `GetBoardsOperation`
    - Вызвать `facade.getBoards({ filter: 'active' })`
    - Проверить: operation вызван с параметрами
    - Проверить: результат (массив досок) возвращен
  - [ ] Тест: `getBoard(boardId, params?)`
    - Создать мок для `GetBoardOperation`
    - Вызвать `facade.getBoard('1', { expand: 'sprints' })`
    - Проверить: operation вызван с boardId и params
  - [ ] Тест: `createBoard(input)`
    - Создать мок для `CreateBoardOperation`
    - Подготовить input: `{ name: 'Sprint Board', filter: {...} }`
    - Вызвать `facade.createBoard(input)`
    - Проверить: делегирование корректно
  - [ ] Тест: `updateBoard(boardId, input)`
    - Создать мок для `UpdateBoardOperation`
    - Вызвать `facade.updateBoard('1', { name: 'Updated Board' })`
    - Проверить: operation вызван с правильными параметрами
  - [ ] Тест: `deleteBoard(boardId)`
    - Создать мок для `DeleteBoardOperation`
    - Вызвать `facade.deleteBoard('1')`
    - Проверить: operation вызван

**Примечание:** Если операции для Boards еще не реализованы, пропустить или создать заглушки.

### 5. Непокрытые методы: Sprints (строки 1034-1079)

**Цель:** Проверить делегирование к sprint operations

- [ ] **describe('Sprint methods'):**
  - [ ] Тест: `getSprints(boardId)`
    - Создать мок для `GetSprintsOperation`
    - Вызвать `facade.getSprints('1')`
    - Проверить: operation вызван с boardId
    - Проверить: результат (массив спринтов) возвращен
  - [ ] Тест: `getSprint(sprintId)`
    - Создать мок для `GetSprintOperation`
    - Вызвать `facade.getSprint('10')`
    - Проверить: operation вызван с sprintId
  - [ ] Тест: `createSprint(input)`
    - Создать мок для `CreateSprintOperation`
    - Подготовить input: `{ name: 'Sprint 1', startDate: '...', endDate: '...' }`
    - Вызвать `facade.createSprint(input)`
    - Проверить: делегирование корректно
  - [ ] Тест: `updateSprint(sprintId, input)`
    - Создать мок для `UpdateSprintOperation`
    - Вызвать `facade.updateSprint('10', { name: 'Sprint 1 Updated' })`
    - Проверить: operation вызван с правильными параметрами

**Примечание:** Если операции для Sprints еще не реализованы, пропустить или создать заглушки.

### 6. Проверка реализации operations

**ВАЖНО:** Перед написанием тестов нужно проверить, существуют ли операции!

- [ ] Проверить наличие operations в `src/tracker_api/api_operations/`:
  - [ ] `worklog/` — GetWorklogsOperation, AddWorklogOperation, UpdateWorklogOperation, DeleteWorklogOperation
  - [ ] `field/` — GetFieldsOperation, GetFieldOperation, CreateFieldOperation, UpdateFieldOperation, DeleteFieldOperation
  - [ ] `board/` — GetBoardsOperation, GetBoardOperation, CreateBoardOperation, UpdateBoardOperation, DeleteBoardOperation
  - [ ] `sprint/` — GetSprintsOperation, GetSprintOperation, CreateSprintOperation, UpdateSprintOperation

**Если операции НЕ существуют:**
- [ ] Пропустить соответствующие тесты (использовать `describe.skip` или `it.skip`)
- [ ] Или создать минимальные моки-заглушки для тестирования facade
- [ ] Документировать в комментариях: "TODO: реализовать operations"

### 7. Валидация и финализация

- [ ] Запустить тесты: `npm test yandex-tracker.facade.test.ts`
- [ ] Проверить покрытие: должно быть ≥ 80%
- [ ] Убедиться, что строки 593-654, 793-1078 теперь покрыты (или пропущены, если operations нет)
- [ ] Исправить failing тесты
- [ ] Проверить ESLint: `npm run lint`
- [ ] Проверить TypeScript: `npm run typecheck`

---

## Архитектурные требования

### Паттерн тестирования Facade
- Facade тесты проверяют ТОЛЬКО делегирование к operations
- НЕ проверяют бизнес-логику (это задача operation tests)
- Структура теста:
  1. Создать мок operation через DI контейнер или `vi.spyOn`
  2. Вызвать метод facade
  3. Проверить, что operation вызван с правильными параметрами
  4. Проверить, что результат operation возвращен

### Использование моков
- Использовать существующие mock factories из `tests/helpers/mock-factories.ts`
- Если нужно мокировать новые operations — добавить в mock-factories
- Альтернатива: использовать `vi.spyOn` для моков методов

### Обработка несуществующих operations
- Если operation не реализована — использовать `it.skip` или `describe.skip`
- Добавить комментарий: `// TODO: реализовать операцию`
- Не создавать фейковые тесты, которые всегда проходят

---

## Критерии готовности

✅ Все реализованные методы покрыты тестами
✅ Покрытие yandex-tracker.facade.ts ≥ 80%
✅ Для несуществующих operations добавлены `skip` тесты или комментарии
✅ Добавлено ~20 новых тест-кейсов (или меньше, если operations нет)
✅ Нет ESLint/TypeScript ошибок

---

## Следующий шаг

После завершения:
1. Коммит: `chore: добавить тесты для facade методов (worklog/fields/boards/sprints)`
2. Пуш в ветку
3. Если некоторые operations не реализованы — создать issue для их реализации (опционально)
