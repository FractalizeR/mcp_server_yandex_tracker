# Этап 3.1: Edge cases для file-upload.util.ts (Parallel)

**Цель:** Дополнить тесты edge cases для file-upload.util.ts (69% → 90%+)

**Тип выполнения:** Parallel (можно выполнять одновременно с 3.2 и 3.3)

**Файл для тестирования:** `packages/servers/yandex-tracker/src/tracker_api/utils/file-upload.util.ts`

**Редактируемый файл:** Найти существующий тестовый файл (если есть) или создать новый

**Оценка времени:** 30-60 минут

**Приоритет:** 🟡 ЖЕЛАТЕЛЬНО

---

## Чек-лист задач

### 1. Подготовка

- [ ] Найти существующий тестовый файл для file-upload.util.ts:
  - Поиск: `find tests -name "*file-upload*"`
  - Если не найден — создать `tests/tracker_api/utils/file-upload.util.test.ts`
- [ ] Прочитать исходный код `src/tracker_api/utils/file-upload.util.ts`
- [ ] Определить, какие функции уже покрыты (69% покрытие)
- [ ] Найти непокрытые строки: 106-111, 132, 144

### 2. Анализ непокрытых участков

**Строки 106-111:** `getFileExtension()` — обработка файлов без расширения

- [ ] Изучить логику функции
- [ ] Определить edge case: файл без точки ('README')
- [ ] Определить edge case: файл с точкой в конце ('file.')
- [ ] Определить edge case: пустая строка

**Строка 132:** `validateFilename()` — проверка на пустое имя файла

- [ ] Изучить логику валидации
- [ ] Определить edge case: пустая строка
- [ ] Определить edge case: строка только из пробелов ('   ')

**Строка 144:** `validateFilename()` — проверка на недопустимые символы

- [ ] Изучить список недопустимых символов: `<>:"|?*`
- [ ] Определить edge case: control characters (0x00-0x1F)
- [ ] Определить edge case: символы, специфичные для OS

### 3. Дополнение тестов для `getFileExtension()`

- [ ] **describe('getFileExtension - Edge Cases'):**
  - [ ] Тест: файл без точки
    - Входные: `'README'`
    - Ожидаемый результат: `''` (пустая строка)
  - [ ] Тест: файл с точкой в конце
    - Входные: `'file.'`
    - Ожидаемый результат: `''`
  - [ ] Тест: пустая строка
    - Входные: `''`
    - Ожидаемый результат: `''`
  - [ ] Тест: только точка
    - Входные: `'.'`
    - Ожидаемый результат: `''`
  - [ ] Тест: множественные точки подряд
    - Входные: `'file..txt'`
    - Проверить поведение

### 4. Дополнение тестов для `validateFilename()`

- [ ] **describe('validateFilename - Edge Cases'):**
  - [ ] Тест: пустая строка
    - Входные: `''`
    - Ожидаемое поведение: выброс ошибки или возврат false
  - [ ] Тест: строка только из пробелов
    - Входные: `'   '`, `'\t'`, `'\n'`
    - Ожидаемое поведение: выброс ошибки (после trim = пустая)
  - [ ] Тест: недопустимые символы Windows
    - Входные: `'file<name>.txt'`, `'file:name.txt'`, `'file|name.txt'`, `'file?name.txt'`, `'file*name.txt'`
    - Ожидаемое поведение: выброс ошибки для каждого
  - [ ] Тест: недопустимые кавычки
    - Входные: `'file"name.txt'`
    - Ожидаемое поведение: выброс ошибки
  - [ ] Тест: control characters (ASCII 0x00-0x1F)
    - Входные: имена с `\0`, `\x01`, `\x1F`
    - Ожидаемое поведение: выброс ошибки
  - [ ] Тест: валидные специальные символы (должны пройти)
    - Входные: `'file-name.txt'`, `'file_name.txt'`, `'file (1).txt'`, `'file.backup.txt'`
    - Ожидаемое поведение: валидация успешна
  - [ ] Тест: unicode символы
    - Входные: `'файл.txt'`, `'文件.txt'`
    - Проверить: разрешены ли unicode имена
  - [ ] Тест: очень длинное имя
    - Входные: имя длиной > 255 символов
    - Проверить: есть ли ограничение по длине

### 5. Дополнительные edge cases (если есть другие функции)

- [ ] Проверить, есть ли другие функции в file-upload.util.ts
- [ ] Если есть `createFormData()` или подобные — добавить edge cases:
  - Пустой Buffer
  - Очень большой файл
  - Невалидный MIME type

### 6. Валидация и финализация

- [ ] Запустить тесты: `npm test file-upload.util.test.ts`
- [ ] Проверить покрытие: должно быть ≥ 90%
- [ ] Убедиться, что строки 106-111, 132, 144 теперь покрыты
- [ ] Исправить failing тесты
- [ ] Проверить ESLint: `npm run lint`
- [ ] Проверить TypeScript: `npm run typecheck`

---

## Архитектурные требования

### Использование существующих паттернов
- Если тестовый файл уже существует — следовать его структуре
- Добавлять новые тесты в соответствующие `describe` блоки
- Не дублировать существующие тесты

### Error Handling
- Использовать `expect().toThrow()` или `expect().toBe(false)` в зависимости от API функции
- Проверять специфичные сообщения об ошибках, если они есть

### Изоляция
- Edge case тесты должны быть независимыми
- Не требуется file system — только валидация строк

---

## Критерии готовности

✅ Все непокрытые строки (106-111, 132, 144) теперь покрыты
✅ Покрытие file-upload.util.ts ≥ 90%
✅ Добавлено ~10 новых тест-кейсов
✅ Нет ESLint/TypeScript ошибок

---

## Следующий шаг

После завершения:
1. Коммит: `chore: дополнить тесты edge cases для file-upload.util.ts (69% → 90%+)`
2. Пуш в ветку
