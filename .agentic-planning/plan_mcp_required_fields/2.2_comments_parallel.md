# 2.2 Comments API - Добавление обязательного `fields` (Parallel)

**Тип:** Parallel

**Приоритет:** Критический (часто используемые tools)

**Количество tools:** 3

---

## Tools для исправления

1. **add_comment** - создание комментария (возвращает Comment)
2. **edit_comment** - редактирование комментария (возвращает Comment)
3. **get_comments** - список комментариев (возвращает Comment[])

**Не требует изменений:**
- `delete_comment` - возвращает void

---

## Файлы для изменения

### 1. add_comment

**Schema:** `src/tools/api/comments/add/add-comment.schema.ts`
- Добавить: `fields: FieldsSchema`

**Tool:** `src/tools/api/comments/add/add-comment.tool.ts`
- Импорт: `ResponseFieldFilter`
- Извлечь: `const { fields, ...commentParams } = validation.data;`
- Фильтрация: `ResponseFieldFilter.filter<CommentWithUnknownFields>(comment, fields)`

**Definition:** `src/tools/api/comments/add/add-comment.definition.ts`
- Добавить описание параметра `fields`

**Тесты:** `tests/tools/api/comments/add-comment.test.ts`
- Добавить `fields: ['id', 'text']` во все вызовы

---

### 2. edit_comment

**Schema:** `src/tools/api/comments/edit/edit-comment.schema.ts`
- Добавить: `fields: FieldsSchema`

**Tool:** `src/tools/api/comments/edit/edit-comment.tool.ts`
- Аналогично `add_comment`

**Definition:** `src/tools/api/comments/edit/edit-comment.definition.ts`
- Обновить описание

**Тесты:** `tests/tools/api/comments/edit-comment.test.ts`
- Добавить `fields`

---

### 3. get_comments

**Schema:** `src/tools/api/comments/get/get-comments.schema.ts`
- Добавить: `fields: FieldsSchema`

**Tool:** `src/tools/api/comments/get/get-comments.tool.ts`
- Фильтрация массива:
```typescript
const filtered = comments.map(comment =>
  ResponseFieldFilter.filter<CommentWithUnknownFields>(comment, fields)
);
```

**Definition:** `src/tools/api/comments/get/get-comments.definition.ts`
- Обновить описание

**Тесты:** `tests/tools/api/comments/get-comments.test.ts`
- Добавить `fields`

---

## Критерии завершения

- [ ] 3 schema файла обновлены
- [ ] 3 tool файла используют `ResponseFieldFilter`
- [ ] 3 definition файла обновлены
- [ ] Тесты обновлены и проходят
- [ ] `npm run validate:quiet` проходит

---

## Коммит

```bash
git add packages/servers/yandex-tracker/src/tools/api/comments/
git add packages/servers/yandex-tracker/tests/tools/api/comments/

git commit -m "feat(comments): добавить обязательный параметр fields во все Comments tools

Детали:
- add_comment, edit_comment, get_comments: добавлен fields
- Используется ResponseFieldFilter для фильтрации
- Обновлены definition и тесты
- Экономия контекста MCP до 80-90%

🤖 Generated with Claude Code
Co-Authored-By: Claude <noreply@anthropic.com>"
```
