# Обновления документации

## CLAUDE.md

### 1. Добавить новую секцию после "Команды (Workspace)"

```markdown
## 🚀 Turborepo

Проект использует Turborepo для управления monorepo.

**Основные команды:**
```bash
# Сборка всех пакетов (автоматический topological order)
turbo run build

# Тесты
turbo run test

# Линтинг
turbo run lint

# Полная валидация
npm run validate

# Очистка кэша
rm -rf .turbo
npm run clean
```

**Преимущества:**
- ✅ Автоматический порядок сборки по графу зависимостей
- ✅ Кэширование результатов (build/test/lint)
- ✅ Параллелизация независимых задач
- ✅ Запуск только измененных пакетов

**Фильтрация:**
```bash
# Собрать только yandex-tracker и его зависимости
turbo run build --filter=@mcp-server/yandex-tracker

# Собрать только изменённые пакеты (с последнего коммита)
turbo run build --filter=[HEAD^1]

# Тесты для изменённых пакетов
turbo run test --filter=[HEAD^1]
```

**Кэш:**
- Локальный кэш: `.turbo/` (в gitignore)
- При повторном запуске без изменений: ~1-2 сек вместо минут
- Очистка: `rm -rf .turbo`

**Конфигурация:** `turbo.json` (в корне monorepo)

**Отладка:**
```bash
# Показать граф задач
turbo run build --graph

# Verbose вывод
turbo run build --verbose

# Принудительно без кэша
turbo run build --force
```
```

---

### 2. Обновить секцию "Команды (Workspace)"

**Заменить:**

```markdown
## 🛠️ Команды (Workspace)

**Корень monorepo:**
```bash
# Установка всех зависимостей
npm install

# Сборка всех пакетов (topological order гарантирован Turborepo)
turbo run build

# Тесты всех пакетов
turbo run test

# Валидация всего monorepo
npm run validate

# Валидация (quiet для ИИ агентов - экономия токенов)
npm run validate:quiet

# Валидация архитектуры (граф зависимостей)
turbo run depcruise

# Очистка всех пакетов
npm run clean
```

**Работа с отдельным пакетом:**
```bash
# Сборка одного пакета (и его зависимостей)
turbo run build --filter=@mcp-framework/core

# Тесты одного пакета
turbo run test --filter=@mcp-server/yandex-tracker

# Из директории пакета (работает как раньше)
cd packages/servers/yandex-tracker
npm test
npm run test:quiet  # для ИИ агентов
```

**ВАЖНО:** Команды через `turbo run` автоматически соблюдают порядок зависимостей!
```

---

## README.md

### Добавить в секцию "Development"

```markdown
## Development

This project uses **Turborepo** for monorepo orchestration, providing:
- ✅ Automatic build ordering
- ✅ Task caching
- ✅ Parallel execution

See [CLAUDE.md](./CLAUDE.md) for detailed development instructions.

**Quick start:**
```bash
# Install dependencies
npm install

# Build all packages
turbo run build

# Run tests
turbo run test

# Validate everything
npm run validate
```
```

---

## Проверка размера CLAUDE.md

После внесения изменений:

```bash
npm run validate:docs
```

Убедиться что CLAUDE.md остался ≤400 строк (лимит).

Если превышен лимит:
1. Удалить примеры кода >10 строк
2. Сократить объяснения
3. Заменить дублирование ссылками

**Целевой размер:** ~350 строк
**Жёсткий лимит:** 400 строк
