# 🎯 План миграции на Turborepo

**Дата создания:** 2025-11-20
**Статус:** Готов к выполнению
**Ориентировочное время:** 1-1.5 часа
**Риск:** 🟢 Низкий

---

## 📋 Быстрый старт

Для запуска миграции в новой сессии:

```bash
# 1. Прочитать этот файл
cat .agentic-planning/turborepo-migration/README.md

# 2. Следовать плану из plan.md
cat .agentic-planning/turborepo-migration/plan.md

# 3. Использовать готовые конфигурации из этой директории
```

---

## 📁 Структура плана

```
.agentic-planning/turborepo-migration/
├── README.md                    # Этот файл (обзор)
├── plan.md                      # Детальный пошаговый план
├── turbo.json                   # Готовая конфигурация Turborepo
├── package.json.changes.md      # Изменения для корневого package.json
├── ci-workflow.yml              # Обновленный CI workflow
└── documentation-updates.md     # Изменения для CLAUDE.md
```

---

## 🎯 Что делает миграция

**Проблемы, которые решаем:**
1. ❌ Порядок сборки не гарантирован (может собрать core до infrastructure)
2. ❌ Нет кэширования (каждый раз пересборка всего)
3. ❌ Нет параллелизации независимых пакетов
4. ❌ CI/CD всегда запускает все тесты (даже если пакет не изменился)

**Что получаем:**
1. ✅ Автоматический topological order при сборке
2. ✅ Кэширование результатов (build/test/lint) - повторные запуски ~1-2 сек
3. ✅ Параллелизация независимых задач
4. ✅ CI/CD ускорение в 2-3 раза за счёт кэша
5. ✅ Единообразный интерфейс через `turbo run`

---

## 📊 Анализ текущего состояния

**Структура monorepo:**
```
packages/
├── framework/
│   ├── infrastructure/  (0 зависимостей)
│   ├── core/            (→ infrastructure)
│   └── search/          (→ core, infrastructure)
└── servers/
    └── yandex-tracker/  (→ все framework пакеты)
```

**Команды для миграции:**
- **Package-level:** build, test, lint, typecheck (выполняются в каждом пакете)
- **Root-level:** cpd, depcruise, validate:docs, knip (выполняются один раз)
- **Исключения:** format, clean, audit:* (остаются через npm)

**Конфигурация:**
- ✅ npm workspaces (остается)
- ✅ TypeScript project references (остается)
- ✅ Dependency Cruiser (остается)
- ➕ Turborepo (добавляется)

---

## ⚠️ Важные моменты

**Что НЕ изменится:**
- ✅ npm publish работает как раньше
- ✅ npx команды работают как раньше
- ✅ Скрипты в пакетах не меняются
- ✅ Pre-commit hooks продолжат работать

**Что изменится:**
- 🔄 Корневой package.json скрипты (используют `turbo run`)
- 🔄 CI/CD workflows (добавлено кэширование)
- ➕ Добавится `.turbo/` в .gitignore
- ➕ Добавится `turbo.json` конфигурация

---

## 🚀 Этапы миграции (краткий обзор)

1. **Установка** (5 мин) - установить turbo, обновить .gitignore
2. **Конфигурация** (20 мин) - создать turbo.json, обновить package.json
3. **CI/CD** (10 мин) - обновить workflows с кэшированием
4. **Тестирование** (20 мин) - проверить все команды, кэш, фильтры
5. **Документация** (15 мин) - обновить CLAUDE.md и README.md
6. **Коммит** (10 мин) - финальная проверка и push

**Итого:** ~1-1.5 часа

---

## 📝 Чек-лист перед началом

- [ ] Прочитать `plan.md` полностью
- [ ] Убедиться что все тесты проходят: `npm run validate`
- [ ] Создать новую ветку или работать в текущей
- [ ] Убедиться что нет незакоммиченных изменений: `git status`

---

## 🆘 Что делать при проблемах

**Проблема: Турборепо не находит задачу**
```bash
Error: Could not find the following tasks in project: cpd
```
**Решение:** Убедиться что скрипт есть в корневом package.json

**Проблема: Кэш не работает**
```bash
turbo run build  # всегда пересобирает
```
**Решение:** Проверить `inputs` в turbo.json для задачи

**Проблема: CI не использует кэш**
**Решение:** Проверить что actions/cache настроен в workflow

**Откат изменений:**
```bash
git reset --hard HEAD
git clean -fd
```

---

## 📚 Полезные ссылки

- **План миграции:** `plan.md` (в этой директории)
- **Turborepo документация:** https://turbo.build/repo/docs
- **Текущая документация:** `CLAUDE.md`, `ARCHITECTURE.md`

---

## ✅ После выполнения

После успешной миграции:

1. Проверить что все работает: `npm run validate`
2. Проверить кэширование: запустить `turbo run build` дважды
3. Закоммитить и запушить изменения
4. Отметить этот план как выполненный (переименовать в `COMPLETED_*.md`)

---

**Готов к выполнению!** 🚀

Следуйте `plan.md` для детальных инструкций.
